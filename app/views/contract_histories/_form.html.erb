<%= form_for(@contract_history) do |f| %>
  <% if @contract_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contract_history.errors.count, "error") %> prohibited this contract_history from being saved:</h2>

      <ul>
      <% @contract_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :personId %><br>
    <%= f.number_field :personId %>
  </div>
  <div class="field">
    <%= f.label :contractDate %><br>
    <%= f.date_select :contractDate %>
  </div>
  <div class="field">
    <%= f.label :finalContractDate %><br>
    <%= f.date_select :finalContractDate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
